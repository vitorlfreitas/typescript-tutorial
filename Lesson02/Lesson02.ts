// Read the Lesson.md file

console.log("Hello, World!");

let age: number = 23;
// Uncomment the next line, you will see that the RunTime Compiler is already working, and it will say "Type 'string' is not assignable to type 'number'.ts(2322)"
// age = "Hello";

// How to define the type
let newAge: number = 20;
let bool: boolean = true;
let string: string = "Hello, World!"

// Open the terminal and use tsc fileName.ts 

// The compiler will create a file.js with your code compiled
